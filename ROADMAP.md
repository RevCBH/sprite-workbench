# Roadmap

## M1 (Current)

- Client-only web app
- No bundled local API server
- Video -> timeline curation -> export bundle
- Export bundle includes sheet PNG, atlas JSON, Godot `.tres`, manifest JSON
- GIF export retained

## v1 (Next)

- Add adapter interfaces for external workflows
- Add optional server-backed adapter package
- Restore sequence/project workflow via adapters (not core)
- Keep core clip-first mode as default
